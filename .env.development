# =================================================================
# 환경 변수 템플릿
# 프로젝트: 광남동성당 청소년위원회 예결산 관리 시스템
# 설명: 개발/테스트/프로덕션 환경 설정 템플릿
# =================================================================
# 
# 사용법:
# 1. 이 파일을 .env.development 로 복사
# 2. 각 변수를 실제 값으로 설정
# 3. .env 파일은 절대 Git에 커밋하지 마세요!
#
# =================================================================

# ==== 환경 설정 ====
NODE_ENV=development
API_PREFIX=api
PORT=8000

# ==== 데이터베이스 설정 (PostgreSQL) ====
DB_HOST=database
DB_PORT=5432
DB_NAME=recipt_dev
DB_USER=recipt_dev
DB_PASSWORD=password123
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# ==== Redis 설정 ====
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_URL=redis://${REDIS_HOST}:${REDIS_PORT}

# ==== JWT 인증 설정 ====
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=1d
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-this-too
JWT_REFRESH_EXPIRES_IN=7d

# ==== 암호화 설정 ====
ENCRYPTION_KEY=your-32-character-encryption-key-here
BCRYPT_ROUNDS=10

# ==== CORS 설정 ====
ENABLE_CORS=true
CORS_ORIGIN=http://localhost:3000,http://localhost:3001
CORS_CREDENTIALS=true

# ==== Swagger API 문서 ====
ENABLE_SWAGGER=true
SWAGGER_PATH=api/docs

# ==== 로깅 설정 ====
LOG_LEVEL=debug
LOG_FILE_PATH=logs/app.log
LOG_MAX_FILES=5
LOG_MAX_SIZE=10m

# ==== OCR 서비스 설정 ====
OCR_SERVICE_URL=http://ocr-service:8001
OCR_SERVICE_TIMEOUT=30000
TESSERACT_LANG=kor+eng
ALLOWED_EXTENSIONS=["jpg","jpeg","png","pdf","tiff","bmp","gif"]
MAX_FILE_SIZE=10485760
UPLOAD_DIR=uploads
PROCESSED_DIR=processed_images

# ==== 파일 업로드 설정 ====
MAX_FILE_SIZE_MB=10
ALLOWED_MIME_TYPES=image/jpeg,image/png,image/tiff,image/bmp,image/gif,application/pdf
UPLOAD_PATH=./uploads
TEMP_PATH=./temp

# ==== 이메일 설정 (선택) ====
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=jopark@yencorp.co.kr
SMTP_PASSWORD=your-email-password
EMAIL_FROM=noreply@recipt-system.com

# ==== 외부 API 설정 ====
# 예: 결제, 알림, 클라우드 서비스 등
EXTERNAL_API_KEY=your-external-api-key
EXTERNAL_API_URL=https://api.example.com
EXTERNAL_API_TIMEOUT=5000

# ==== 보안 설정 ====
RATE_LIMIT_WINDOW=15
RATE_LIMIT_MAX=100
RATE_LIMIT_SKIP_SUCCESSFUL_REQUESTS=true
HELMET_ENABLED=true
CSRF_ENABLED=false

# ==== 세션 설정 ====
SESSION_SECRET=your-session-secret-key
SESSION_MAX_AGE=3600000
COOKIE_SECURE=false
COOKIE_HTTP_ONLY=true

# ==== 건강 상태 체크 설정 ====
HEALTH_CHECK_DATABASE=true
HEALTH_CHECK_REDIS=true
HEALTH_CHECK_OCR_SERVICE=true

# ==== 개발 도구 설정 ====
HOT_RELOAD=true
WATCH_FILES=true
DEBUG_MODE=true
PROFILING_ENABLED=false

# ==== 캐싱 설정 ====
CACHE_TTL=3600
CACHE_MAX_ITEMS=1000
ENABLE_CACHE=true

# ==== 백업 설정 ====
BACKUP_ENABLED=true
BACKUP_SCHEDULE=0 2 * * *
BACKUP_RETENTION_DAYS=30
BACKUP_PATH=./backups

# ==== 모니터링 설정 ====
METRICS_ENABLED=true
METRICS_PORT=9090
PROMETHEUS_ENABLED=false

# ==== 언어 및 지역 설정 ====
DEFAULT_LANGUAGE=ko
SUPPORTED_LANGUAGES=ko,en
TIMEZONE=Asia/Seoul
DATE_FORMAT=YYYY-MM-DD
CURRENCY=KRW

# ==== 프론트엔드 설정 ====
FRONTEND_URL=http://localhost:3000
API_URL=http://localhost:8000/api

# ==== 개발 환경 전용 설정 ====
SEED_DATA_ENABLED=true
MOCK_DATA_ENABLED=true
TEST_USER_EMAIL=test@example.com
TEST_USER_PASSWORD=test123

# =================================================================
# 주의사항:
# 1. 프로덕션 환경에서는 모든 시크릿을 안전한 값으로 변경하세요
# 2. 데이터베이스 비밀번호는 강력한 비밀번호를 사용하세요
# 3. JWT 시크릿은 최소 32자 이상의 랜덤 문자열을 사용하세요
# 4. CORS 설정은 실제 도메인으로 제한하세요
# 5. 로그 레벨은 프로덕션에서 'info' 또는 'warn'으로 설정하세요
# =================================================================